provider: openai           # openai | kubeai
authMode: accept           # accept | reject-401
image:
  repository: litellm-mock
  tag: e2e
  pullPolicy: Never        # kind-loaded
fixtures:
  modelsJSON: ""           # if non-empty, overrides binary's embedded fixture
service:
  port: 8080
