apiVersion: v1
kind: Secret
metadata:
  name: litellm-master-key
  namespace: default
type: Opaque
stringData:
  # MUST equal masterkey in test/e2e/values/litellm.values.yaml.
  # Twin Secrets: chart creates one in litellm-system for inbound auth;
  # this fixture creates one in default for the operator's outbound auth.
  master-key: sk-test-master-key
