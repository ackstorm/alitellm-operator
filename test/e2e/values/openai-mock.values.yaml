# Tier 2 openai-compatible mock instance
# Used by:  scripts/cluster.sh up (step 6)
# Chart:    test/e2e/charts/mocks/
provider: openai
authMode: accept           # tests flip at runtime via /__mock/auth-mode
image:
  repository: litellm-mock
  tag: e2e
  pullPolicy: Never
fixtures:
  modelsJSON: ""           # use binary's embedded openai-models.json
